# Bot Testing Guide

## Overview

The `bot_simulator.py` script simulates different types of bots trying to bypass your passive bot detection system. It tests whether your system correctly identifies and blocks bots.

---

## Prerequisites

✅ Backend running
```bash
cd backend
python -m ml_engine.inference.server